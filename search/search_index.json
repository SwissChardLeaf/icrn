{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"icrn","text":"<p>Indexed Chemical Reaction Networks in a differentiable, tensor-based framework.</p> <p><code>icrn</code> is a JAX library for specifying chemical reaction networks with indexed species and rate constants, and simulating them as either well-mixed ODEs or reaction\u2013diffusion PDEs. Because everything compiles to <code>jax.numpy</code> operations, simulations are JIT-able with <code>jax.jit</code>, batchable with <code>jax.vmap</code>, and differentiable end-to-end with <code>jax.grad</code> \u2014 so rate constants and initial conditions can be trained.</p> <p>Note</p> <p>Research code; interfaces are subject to change.</p>"},{"location":"#where-to-start","title":"Where to start","text":"<ul> <li>Getting started \u2014 installation and quickstart   examples for well-mixed, indexed, and reaction-diffusion systems, plus a   taste of differentiable simulation.</li> <li>API reference \u2014 the full public surface of <code>icrn</code>.</li> <li>Developer notes \u2014 layout, tests, and tooling for   library contributors.</li> <li>Changelog   on GitHub.</li> </ul>"},{"location":"#install","title":"Install","text":"<pre><code>pip install icrn\n</code></pre> <p>For GPU/TPU acceleration, follow the JAX install guide; the default wheel is CPU-only.</p>"},{"location":"#cite","title":"Cite","text":"<p>If you use <code>icrn</code> in academic work, please cite the DNA 31 paper:</p> <p>Inhoo Lee, Salvador Buse, and Erik Winfree. Differentiable Programming of Indexed Chemical Reaction Networks and Reaction-Diffusion Systems. In 31st International Conference on DNA Computing and Molecular Programming (DNA 31), LIPIcs Vol. 347, pp. 4:1\u20134:23. Schloss Dagstuhl, 2025. https://doi.org/10.4230/LIPIcs.DNA.31.4</p>"},{"location":"api/","title":"API reference","text":"<p>The full public API of <code>icrn</code>. Everything below is also accessible directly from the top-level package, e.g. <code>from icrn import MassActionReaction</code>.</p>"},{"location":"api/#symbols-and-expressions","title":"Symbols and expressions","text":""},{"location":"api/#icrn.IndexSymbol","title":"IndexSymbol","text":"<pre><code>IndexSymbol(label: str, index_set: int = 0)\n</code></pre> <p>               Bases: <code>OrderedHashableSymbol</code></p> <p>A named index over a finite range, used to subscript species and rate constants.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Display name of the index (e.g. <code>\"i\"</code>, <code>\"j\"</code>).</p> required <code>index_set</code> <code>int</code> <p>Size of the index set. <code>0</code> (the default) means the size is unspecified and will be inferred at solve time from the shape of the data bound to species/rate-constant tensors.</p> <code>0</code> <p>Attributes:</p> Name Type Description <code>label</code> <code>str</code> <p> <code>index_set</code> <code>int</code> <p> <p>Examples:</p> <pre><code># &lt;TODO: minimal usage example, e.g. constructing an IndexSymbol\n# and using it to subscript a Species.&gt;\n</code></pre> Source code in <code>icrn/symbols.py</code> <pre><code>def __init__(self, label: str, index_set: int = 0):\n    if not isinstance(label, str):\n        raise ValueError(f\"Label must be a string, got {type(label)}\")\n    if index_set:\n        if not isinstance(index_set, int):\n            raise ValueError(\n                f\"Index set must be an integer, got {type(index_set)}\"\n            )\n        if index_set &lt; 0:\n            raise ValueError(\n                f\"Index set must be greater than or equal to 0, \"\n                f\"got {index_set}\"\n            )\n\n    object.__setattr__(self, \"label\", label)\n    object.__setattr__(self, \"aux\", index_set)\n</code></pre>"},{"location":"api/#icrn.Species","title":"Species  <code>dataclass</code>","text":"<pre><code>Species(label: str, indexing: tuple[IndexSymbol, ...] = ())\n</code></pre> <p>               Bases: <code>TensorSymbol</code></p> <p>A chemical species, optionally indexed.</p> <p>Use the <code>+</code> and <code>*</code> operators to build <code>Complex</code> objects (e.g. <code>A + 2 * B</code>), and <code>[i, j]</code> to produce an indexed copy.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p> required <code>indexing</code> <code>tuple of IndexSymbol</code> <p> <code>()</code> <p>Examples:</p> <pre><code># &lt;TODO: e.g. A, B = many_species(\"A, B\"); rxn = A + 2*B -&gt; C&gt;\n</code></pre> Source code in <code>icrn/symbols.py</code> <pre><code>def __init__(self, label: str, indexing: tuple[IndexSymbol, ...] = ()):\n    if not isinstance(label, str):\n        raise ValueError(f\"Label must be a string, got {type(label)}\")\n    object.__setattr__(self, \"label\", label)\n\n    if not isinstance(indexing, tuple):\n        raise ValueError(f\"Indexing must be a tuple, got {type(indexing)}\")\n    # if isinstance(indexing, IndexSymbol):\n    #     indexing = (indexing,)\n    if not all(isinstance(i, IndexSymbol) for i in indexing):\n        raise ValueError(\n            f\"Indexing must be a tuple of IndexSymbols, got {\n                type(indexing)\n            }\"\n        )\n\n    object.__setattr__(self, \"aux\", indexing)\n</code></pre>"},{"location":"api/#icrn.RateConstant","title":"RateConstant  <code>dataclass</code>","text":"<pre><code>RateConstant(\n    label: str, indexing: tuple[IndexSymbol, ...] = ()\n)\n</code></pre> <p>               Bases: <code>TensorSymbol</code></p> <p>A named rate constant, optionally indexed.</p> <p>Bound to numeric values at solve time via the <code>rate_constant_vals</code> argument of the solver functions.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p> required <code>indexing</code> <code>tuple of IndexSymbol</code> <p> <code>()</code> <p>Examples:</p> <pre><code># &lt;TODO: e.g. k = many_rate_constants(\"k\"); pass {k: jnp.array(1.0)}\n# as rate_constant_vals.&gt;\n</code></pre> Source code in <code>icrn/symbols.py</code> <pre><code>def __init__(self, label: str, indexing: tuple[IndexSymbol, ...] = ()):\n    if not isinstance(label, str):\n        raise ValueError(f\"Label must be a string, got {type(label)}\")\n    object.__setattr__(self, \"label\", label)\n\n    if not isinstance(indexing, tuple):\n        raise ValueError(f\"Indexing must be a tuple, got {type(indexing)}\")\n    # if isinstance(indexing, IndexSymbol):\n    #     indexing = (indexing,)\n    if not all(isinstance(i, IndexSymbol) for i in indexing):\n        raise ValueError(\n            f\"Indexing must be a tuple of IndexSymbols, got {\n                type(indexing)\n            }\"\n        )\n\n    object.__setattr__(self, \"aux\", indexing)\n</code></pre>"},{"location":"api/#icrn.Complex","title":"Complex  <code>dataclass</code>","text":"<pre><code>Complex(count_dict: dict)\n</code></pre> <p>A multiset of <code>Species</code>, used as the reactant or product side of a reaction.</p> <p>Complexes are built ergonomially with opertaors involving <code>Species</code> (e.g. <code>A + 2 * B</code> produces <code>Complex({A: 1, B: 2})</code>). A complex is unlikely to be constructed directly.</p> <p>Parameters:</p> Name Type Description Default <code>count_dict</code> <code>dict[Species, int]</code> <p> required <p>Attributes:</p> Name Type Description <code>count_dict</code> <code>dict[Species, int]</code> <p> <p>Examples:</p> <pre><code># &lt;TODO: show that A + 2 * B yields a Complex with count_dict\n# {A: 1, B: 2}.&gt;\n</code></pre>"},{"location":"api/#icrn.TensorExpression","title":"TensorExpression","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for tensor-valued expressions over indexed symbols.</p> <p>A <code>TensorExpression</code> is anything that, given a binding of symbols to concrete arrays, evaluates to a JAX array of a known indexed shape. Concrete subclasses include <code>Species</code>, <code>RateConstant</code>, <code>TensorLiteral</code>, and <code>TensorFunction</code>.</p> Notes <p>+, <code>*</code>, <code>-</code>, <code>/</code>) compose <code>TensorExpression</code> instances into new ones, and how <code>index_symbols</code> propagates through these operations.&gt; See Also <p><code>TensorLiteral</code> : Wraps a scalar numeric value. <code>TensorFunction</code> : Lifts a JAX function into a     <code>TensorExpression</code>.</p>"},{"location":"api/#icrn.TensorSymbol","title":"TensorSymbol","text":"<pre><code>TensorSymbol(\n    label: str, indexing: tuple[IndexSymbol, ...] = ()\n)\n</code></pre> <p>               Bases: <code>OrderedHashableSymbol</code>, <code>TensorExpression</code></p> Source code in <code>icrn/symbols.py</code> <pre><code>def __init__(self, label: str, indexing: tuple[IndexSymbol, ...] = ()):\n    if not isinstance(label, str):\n        raise ValueError(f\"Label must be a string, got {type(label)}\")\n    object.__setattr__(self, \"label\", label)\n\n    if not isinstance(indexing, tuple):\n        raise ValueError(f\"Indexing must be a tuple, got {type(indexing)}\")\n    # if isinstance(indexing, IndexSymbol):\n    #     indexing = (indexing,)\n    if not all(isinstance(i, IndexSymbol) for i in indexing):\n        raise ValueError(\n            f\"Indexing must be a tuple of IndexSymbols, got {\n                type(indexing)\n            }\"\n        )\n\n    object.__setattr__(self, \"aux\", indexing)\n</code></pre>"},{"location":"api/#icrn.TensorLiteral","title":"TensorLiteral  <code>dataclass</code>","text":"<pre><code>TensorLiteral(numeric_value)\n</code></pre> <p>               Bases: <code>TensorExpression</code></p> <p>A scalar numeric constant lifted into a <code>TensorExpression</code>.</p> <p>TensorLiterals allow for python/JAX scalars to interact with other symbols to form expressions.</p> <p>Parameters:</p> Name Type Description Default <code>numeric_value</code> <code>Numeric</code> <p> required <p>Attributes:</p> Name Type Description <code>numeric_value</code> <code>Array</code> <p> <p>Examples:</p> <pre><code># &lt;TODO: e.g. TensorLiteral(2.5).eval({}) -&gt; 2.5&gt;\n</code></pre> Source code in <code>icrn/symbols.py</code> <pre><code>def __init__(self, numeric_value):\n    if not isinstance(numeric_value, Numeric):\n        raise ValueError(\n            f\"Numeric value must be a Numeric, got {type(numeric_value)}\"\n        )\n\n    val_as_array = jnp.array(numeric_value).astype(float)\n    if val_as_array.ndim &gt; 0:\n        raise ValueError(\n            f\"Numeric value must be a scalar, got {val_as_array.shape}\"\n        )\n\n    object.__setattr__(self, \"numeric_value\", val_as_array)\n</code></pre>"},{"location":"api/#icrn.TensorFunction","title":"TensorFunction  <code>dataclass</code>","text":"<pre><code>TensorFunction(fn, args)\n</code></pre> <p>               Bases: <code>TensorExpression</code></p> <p>A symbolic representation of a JAX function applied to one or more arguments.</p> <p>Representing the function symbolically allows for tensor symbol arguments to be indexed, and the function to be applied to the arguments at evaluation time. Currently, all arguments have the same index symbols.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>callable</code> <p>a JAX-compatible callable which takes a number of arguments equal to the length of <code>args</code> and returns a scalar.</p> required <code>args</code> <code>tuple of TensorExpression</code> <p>arguments passed to <code>fn</code> at evaluation time. Must share a single set of <code>index_symbols</code>.</p> required <p>Attributes:</p> Name Type Description <code>fn</code> <code>callable</code> <code>args</code> <code>tuple of TensorExpression</code> See Also <p><code>TensorExpression</code> : Abstract base class for     tensor-valued expressions over indexed symbols. <code>TensorLiteral</code> : Wraps a scalar numeric value. <code>TensorSymbol</code> : Indexed array-like symbol. <code>RateConstant</code> : A named rate constant, optionally     indexed.</p> <p>Examples:</p> <pre><code># &lt;TODO: e.g. relu_x = TensorFunction(jax.nn.relu, (x,)), used inside\n# a MassActionReaction rate expression.&gt;\n</code></pre> Source code in <code>icrn/symbols.py</code> <pre><code>def __init__(self, fn, args):\n    if not isinstance(fn, Callable):\n        raise ValueError(f\"Function must be callable, got {type(fn)}\")\n\n    if not isinstance(args, tuple):\n        args = (args,)\n\n    if isinstance(fn, jnp.ufunc):\n        if fn.nin != len(args):\n            raise ValueError(\n                f\"Function must have {fn.nin} arguments, got {len(args)}\"\n            )\n    for arg in args:\n        _index_symbols_for_arg(arg)\n    _unify_index_symbols(args)\n\n    object.__setattr__(self, \"fn\", fn)\n    object.__setattr__(self, \"args\", args)\n</code></pre>"},{"location":"api/#constructors","title":"Constructors","text":""},{"location":"api/#icrn.many_index_symbols","title":"many_index_symbols","text":"<pre><code>many_index_symbols(\n    names: str, index_set: int = 0\n) -&gt; IndexSymbol | tuple[IndexSymbol]\n</code></pre> <p>Instanitate multiple index symbols with the same index sets at once.</p> <p>Parameters:</p> Name Type Description Default <code>names</code> <code>str</code> <p>A single string with index symbol comma-separated names.</p> required <code>index_set</code> <code>int</code> <p>The upper limit of index sets for each index symbol.</p> <code>0</code> <p>Returns:</p> Type Description <code>a single IndexSymbol or a tuple of IndexSymbols</code> <p>Order of IndexSymbols is based on their order in <code>names</code>.</p> Source code in <code>icrn/symbols.py</code> <pre><code>def many_index_symbols(\n    names: str, index_set: int = 0\n) -&gt; IndexSymbol | tuple[IndexSymbol]:\n    \"\"\"\n    Instanitate multiple index symbols with the same index sets at once.\n\n    Parameters\n    ----------\n    names : str\n        A single string with index symbol comma-separated names.\n    index_set : int\n        The upper limit of index sets for each index symbol.\n\n    Returns\n    -------\n    a single IndexSymbol or a tuple of IndexSymbols\n        Order of IndexSymbols is based on their order in `names`.\n    \"\"\"\n    try:\n        name_list = _parse_names(names)\n    except AttributeError:\n        raise ValueError(f\"Invalid index symbol names: {names}\")\n\n    if len(name_list) == 1:\n        return IndexSymbol(name_list[0], index_set)\n    else:\n        return tuple(map(lambda name: IndexSymbol(name, index_set), name_list))\n</code></pre>"},{"location":"api/#icrn.many_species","title":"many_species","text":"<pre><code>many_species(names: str) -&gt; Species | tuple[Species]\n</code></pre> <p>Instanitate multiple Species at once.</p> <p>Parameters:</p> Name Type Description Default <code>names</code> <code>str</code> <p>Single string with Species names comma-separated or space-separated.</p> required <p>Returns:</p> Type Description <code>a single Species or a tuple of Species</code> <p>Order of Species is based on their order in <code>names</code>.</p> Source code in <code>icrn/symbols.py</code> <pre><code>def many_species(names: str) -&gt; Species | tuple[Species]:\n    \"\"\"\n    Instanitate multiple Species at once.\n\n    Parameters\n    ----------\n    names : str\n        Single string with Species names comma-separated or space-separated.\n\n    Returns\n    -------\n    a single Species or a tuple of Species\n        Order of Species is based on their order in `names`.\n    \"\"\"\n    try:\n        name_list = _parse_names(names)\n    except AttributeError:\n        raise ValueError(f\"Invalid species names: {names}\")\n\n    if len(name_list) == 1:\n        return Species(name_list[0])\n    else:\n        return tuple(map(lambda name: Species(name), name_list))\n</code></pre>"},{"location":"api/#icrn.many_rate_constants","title":"many_rate_constants","text":"<pre><code>many_rate_constants(\n    names: str,\n) -&gt; RateConstant | tuple[RateConstant]\n</code></pre> <p>Instanitate multiple RateConstants at once.</p> <p>Parameters:</p> Name Type Description Default <code>names</code> <code>str</code> <p>Single string with RateConstant names comma-separated or space-separated.</p> required <p>Returns:</p> Type Description <code>a single RateConstant or a tuple of RateConstants</code> <p>Order of RateConstants is based on their order in <code>names</code>.</p> Source code in <code>icrn/symbols.py</code> <pre><code>def many_rate_constants(names: str) -&gt; RateConstant | tuple[RateConstant]:\n    \"\"\"\n    Instanitate multiple RateConstants at once.\n\n    Parameters\n    ----------\n    names : str\n        Single string with RateConstant names comma-separated or\n        space-separated.\n\n    Returns\n    -------\n    a single RateConstant or a tuple of RateConstants\n        Order of RateConstants is based on their order in `names`.\n    \"\"\"\n    try:\n        name_list = _parse_names(names)\n    except AttributeError:\n        raise ValueError(f\"Invalid species names: {names}\")\n\n    if len(name_list) == 1:\n        return RateConstant(name_list[0])\n    else:\n        return tuple(map(lambda name: RateConstant(name), name_list))\n</code></pre>"},{"location":"api/#reactions","title":"Reactions","text":""},{"location":"api/#icrn.AbstractReaction","title":"AbstractReaction  <code>dataclass</code>","text":"<pre><code>AbstractReaction(\n    reactants: Complex,\n    products: Complex,\n    aux: TensorExpression | None = None,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base for reactions.</p> <p>Subclasses must implement <code>flux()</code>, which returns a callable mapping <code>(state, non_state)</code> to a per-species flux dictionary. Users looking for non mass action kinetics should extend this class.</p> <p>Parameters:</p> Name Type Description Default <code>reactants</code> <code>Complex</code> <p> required <code>products</code> <code>Complex</code> <p> required <code>aux</code> <code>TensorExpression or None</code> <p><code>MassActionReaction</code>.&gt; <code>None</code> <p>Attributes:</p> Name Type Description <code>reactants</code> <code>Complex</code> <p> <code>products</code> <code>Complex</code> <p> <code>aux</code> <code>TensorExpression or None</code> <p> See Also <p><code>MassActionReaction</code> : Standard mass-action     reaction. <code>FastReaction</code> : Limiting-reagent annihilation     reaction.</p>"},{"location":"api/#icrn.MassActionReaction","title":"MassActionReaction","text":"<pre><code>MassActionReaction(\n    reactants: Complex,\n    products: Complex,\n    rate_expr: TensorExpression,\n)\n</code></pre> <p>               Bases: <code>AbstractReaction</code></p> <p>A mass-action reaction with a (possibly indexed) rate expression.</p> <p>The instantaneous flux of the reaction at species concentrations \\(x\\) is <code>rate_expr * prod(x[s] ** count for s, count in reactants)</code>, contributing \\(+(c_\\text{product} - c_\\text{reactant}) \\cdot \\text{flux}\\) to each species (with \\(c\\) the stoichiometric coefficient).</p> <p>Parameters:</p> Name Type Description Default <code>reactants</code> <code>Complex or Species or 0</code> <p>0 is accepted as an empty complex, allowing zeroth-order production reactions.&gt; required <code>products</code> <code>Complex or Species or 0</code> <p> required <code>rate_expr</code> <code>TensorExpression or Numeric</code> <p><code>TensorLiteral</code>) or an indexed <code>RateConstant</code> / <code>TensorExpression</code>.&gt; required <p>Attributes:</p> Name Type Description <code>reactants</code> <code>Complex</code> <p> <code>products</code> <code>Complex</code> <p> <code>rate_expr</code> <code>TensorExpression</code> <p>aux.&gt; <p>Examples:</p> <pre><code># &lt;TODO: e.g. MassActionReaction(A, B, k) for A --k--&gt; B.&gt;\n</code></pre> Source code in <code>icrn/reactions.py</code> <pre><code>def __init__(\n    self,\n    reactants: Complex,\n    products: Complex,\n    rate_expr: TensorExpression,\n):\n    if not (isinstance(reactants, Complex | Species) or reactants == 0):\n        raise ValueError(\n            f\"Reactants must be a Complex or Species or 0, got {\n                type(reactants)\n            }\"\n        )\n    if isinstance(reactants, Species):\n        reactants = Complex({reactants: 1})\n    elif isinstance(reactants, int) and reactants == 0:\n        reactants = Complex({})\n\n    if not (isinstance(products, Complex | Species) or products == 0):\n        raise ValueError(\n            f\"Products must be a Complex or Species, got {type(products)}\"\n        )\n    if isinstance(products, Species):\n        products = Complex({products: 1})\n    elif isinstance(products, int) and products == 0:\n        products = Complex({})\n\n    if not isinstance(rate_expr, TensorExpression | Numeric) or isinstance(\n        rate_expr, Species\n    ):\n        raise ValueError(\n            f\"Rate expression must be a TensorExpression (not Species)\"\n            f\"or Numeric, got {type(rate_expr)}\"\n        )\n    if isinstance(rate_expr, Numeric):\n        rate_expr = TensorLiteral(rate_expr)\n\n    try:\n        reactants_set = set(reactants.count_dict.keys())\n        products_set = set(products.count_dict.keys())\n        _matching_shapes(reactants_set | products_set)\n    except ValueError:\n        raise\n\n    object.__setattr__(self, \"reactants\", reactants)\n    object.__setattr__(self, \"products\", products)\n    object.__setattr__(self, \"aux\", rate_expr)\n</code></pre>"},{"location":"api/#icrn.FastReaction","title":"FastReaction  <code>dataclass</code>","text":"<pre><code>FastReaction(\n    reactants: Complex | Species,\n    products: Complex | Species | int,\n)\n</code></pre> <p>An infinitely fast reaction.</p> <p>At every integration step, reactions fire until the reactants are fully consumed. They are useful for modelling reactions that are on a timescale much faster than the dt. Currently, a concrete species can be involved in at most one fast reaction.</p> <p>Parameters:</p> Name Type Description Default <code>reactants</code> <code>Complex or Species</code> <p>index_symbols across all reactant species.&gt; required <code>products</code> <code>Complex or Species or 0</code> <p>0 denotes a sink (no products).&gt; required <p>Attributes:</p> Name Type Description <code>reactants</code> <code>Complex</code> <p> <code>products</code> <code>Complex</code> <p> <p>Examples:</p> <pre><code># &lt;TODO: e.g. FastReaction(A + B, C) drives A + B -&gt; C\n# to completion at every step.&gt;\n</code></pre>"},{"location":"api/#solvers","title":"Solvers","text":""},{"location":"api/#icrn.solve_well_mixed","title":"solve_well_mixed","text":"<pre><code>solve_well_mixed(\n    rxns: Iterable[AbstractReaction | FastReaction],\n    conc_vals: dict[Species, ndarray],\n    rate_constant_vals: dict[TensorSymbol, ndarray],\n    times: Numeric,\n    dt: Numeric,\n    key=None,\n    checkpoint_length=None,\n    reaction_solver=\"RK4\",\n    mode: str | None = None,\n)\n</code></pre> <p>Integrate a well-mixed reaction network as a system of ODEs.</p> <p>Parameters:</p> Name Type Description Default <code>rxns</code> <code>iterable of AbstractReaction or FastReaction</code> <p> required <code>conc_vals</code> <code>dict[Species, Array]</code> <p>Species. Array shape must match the species' index sets.&gt; required <code>rate_constant_vals</code> <code>dict[TensorSymbol, Array]</code> <p>RateConstant referenced in <code>rxns</code>.&gt; required <code>times</code> <code>Numeric</code> <p> required <code>dt</code> <code>Numeric</code> <p> required <code>key</code> <code>PRNGKey</code> <p> <code>None</code> <code>checkpoint_length</code> <code>int</code> <p> <code>None</code> <code>reaction_solver</code> <code>(RK4, Euler, PatankarEuler)</code> <p> <code>\"RK4\"</code> <code>mode</code> <code>str or None</code> <p> <code>None</code> <p>Returns:</p> Type Description <code>dict[Species, Array]</code> <p> <p>Examples:</p> <pre><code># &lt;TODO: minimal exponential-decay example.&gt;\n</code></pre> See Also <p><code>solve_reaction_diffusion</code> : Spatial     counterpart with diffusion.</p> Source code in <code>icrn/solver.py</code> <pre><code>def solve_well_mixed(\n    rxns: Iterable[AbstractReaction | FastReaction],\n    conc_vals: dict[Species, jnp.ndarray],\n    rate_constant_vals: dict[TensorSymbol, jnp.ndarray],\n    times: Numeric,\n    dt: Numeric,\n    key=None,\n    checkpoint_length=None,\n    reaction_solver=\"RK4\",\n    mode: str | None = None,\n):\n    \"\"\"Integrate a well-mixed reaction network as a system of ODEs.\n\n    Parameters\n    ----------\n    rxns : iterable of AbstractReaction or FastReaction\n        &lt;TODO: the reaction network to integrate.&gt;\n    conc_vals : dict[Species, jax.Array]\n        &lt;TODO: initial concentrations, keyed by `Species`. Array shape\n        must match the species' index sets.&gt;\n    rate_constant_vals : dict[TensorSymbol, jax.Array]\n        &lt;TODO: numeric values for every `RateConstant` referenced in\n        `rxns`.&gt;\n    times : Numeric\n        &lt;TODO: total integration time.&gt;\n    dt : Numeric\n        &lt;TODO: integrator step size.&gt;\n    key : jax.random.PRNGKey, optional\n        &lt;TODO: required only by stochastic integrators; ignored\n        otherwise.&gt;\n    checkpoint_length : int, optional\n        &lt;TODO: trade memory for time when differentiating long\n        trajectories.&gt;\n    reaction_solver : {\"RK4\", \"Euler\", \"PatankarEuler\"}, optional\n        &lt;TODO: integrator used for reaction dynamics.&gt;\n    mode : str or None, optional\n        &lt;TODO&gt;\n\n    Returns\n    -------\n    dict[Species, jax.Array]\n        &lt;TODO: time-series of concentrations, with a leading time axis.&gt;\n\n    Examples\n    --------\n    ```python\n    # &lt;TODO: minimal exponential-decay example.&gt;\n    ```\n\n    See Also\n    --------\n    [`solve_reaction_diffusion`][icrn.solve_reaction_diffusion] : Spatial\n        counterpart with diffusion.\n    \"\"\"\n    ops = to_well_mixed_ops(rxns, reaction_solver, mode)\n    return solve_with_ops(\n        ops=ops,\n        state=conc_vals,\n        non_state=rate_constant_vals,\n        dt=dt,\n        times=times,\n        key=key,\n        checkpoint_length=checkpoint_length,\n    )\n</code></pre>"},{"location":"api/#icrn.solve_reaction_diffusion","title":"solve_reaction_diffusion","text":"<pre><code>solve_reaction_diffusion(\n    rxns: Iterable[AbstractReaction | FastReaction],\n    conc_vals: dict[Species, ndarray],\n    rate_constant_vals: dict[TensorSymbol, ndarray],\n    diffusion_constant_vals: dict[TensorSymbol, ndarray],\n    times: Numeric,\n    dt: Numeric,\n    spatial_dims: tuple[int],\n    dspaces: tuple[float, ...],\n    key=None,\n    checkpoint_length=None,\n    reaction_solver=\"RK4\",\n    boundary_condition=\"neumann\",\n    splitting=\"LieTrotter\",\n    spatial_rate_constants: bool = False,\n    mode: str | None = None,\n)\n</code></pre> <p>Integrate a reaction-diffusion PDE on a regular grid.</p> <p>Reactions are integrated with the chosen method, diffusion solved with a spectral method, and the two are combined via the chosen splitting scheme.</p> <p>Parameters:</p> Name Type Description Default <code>rxns</code> <code>iterable of AbstractReaction or FastReaction</code> <p> required <code>conc_vals</code> <code>dict[Species, Array]</code> <p> required <code>rate_constant_vals</code> <code>dict[TensorSymbol, Array]</code> <p> required <code>diffusion_constant_vals</code> <code>dict[TensorSymbol, Array]</code> <p>rxns.&gt; required <code>times</code> <code>Numeric</code> <p> required <code>dt</code> <code>Numeric</code> <p> required <code>spatial_dims</code> <code>tuple of int</code> <p>(64, 64).&gt; required <code>dspaces</code> <code>tuple of float</code> <p>spatial_dims.&gt; required <code>key</code> <code>PRNGKey</code> <p> <code>None</code> <code>checkpoint_length</code> <code>int</code> <p> <code>None</code> <code>reaction_solver</code> <code>(RK4, Euler, PatankarEuler)</code> <p> <code>\"RK4\"</code> <code>splitting</code> <code>(LieTrotter, Strang)</code> <p> <code>\"LieTrotter\"</code> <code>spatial_rate_constants</code> <code>bool</code> <p>True, rate constants vary in space and must be supplied as full spatial arrays.&gt; <code>False</code> <code>mode</code> <code>str or None</code> <p> <code>None</code> <p>Returns:</p> Type Description <code>dict[Species, Array]</code> <p> <p>Examples:</p> <pre><code># &lt;TODO: minimal Gray-Scott or Turing example.&gt;\n</code></pre> See Also <p><code>solve_well_mixed</code> : Non-spatial counterpart.</p> Source code in <code>icrn/solver.py</code> <pre><code>def solve_reaction_diffusion(\n    rxns: Iterable[AbstractReaction | FastReaction],\n    conc_vals: dict[Species, jnp.ndarray],\n    rate_constant_vals: dict[TensorSymbol, jnp.ndarray],\n    diffusion_constant_vals: dict[TensorSymbol, jnp.ndarray],\n    times: Numeric,\n    dt: Numeric,\n    spatial_dims: tuple[int],\n    dspaces: tuple[float, ...],\n    key=None,\n    checkpoint_length=None,\n    reaction_solver=\"RK4\",\n    boundary_condition=\"neumann\",\n    splitting=\"LieTrotter\",\n    spatial_rate_constants: bool = False,\n    mode: str | None = None,\n):\n    \"\"\"Integrate a reaction-diffusion PDE on a regular grid.\n\n    Reactions are integrated with the chosen method, diffusion solved with a\n    spectral method, and the two are combined via the chosen splitting scheme.\n\n    Parameters\n    ----------\n    rxns : iterable of AbstractReaction or FastReaction\n        &lt;TODO&gt;\n    conc_vals : dict[Species, jax.Array]\n        &lt;TODO: initial concentrations on the spatial grid; array shape\n        must include both the the spatial axes and the species' index axes.&gt;\n    rate_constant_vals : dict[TensorSymbol, jax.Array]\n        &lt;TODO&gt;\n    diffusion_constant_vals : dict[TensorSymbol, jax.Array]\n        &lt;TODO: diffusion coefficient for each species, keyed by the same\n        symbols used to declare diffusion in `rxns`.&gt;\n    times : Numeric\n        &lt;TODO&gt;\n    dt : Numeric\n        &lt;TODO&gt;\n    spatial_dims : tuple of int\n        &lt;TODO: shape of the spatial grid, e.g. `(64, 64)`.&gt;\n    dspaces : tuple of float\n        &lt;TODO: grid spacing along each spatial axis; same length as\n        `spatial_dims`.&gt;\n    key : jax.random.PRNGKey, optional\n        &lt;TODO&gt;\n    checkpoint_length : int, optional\n        &lt;TODO&gt;\n    reaction_solver : {\"RK4\", \"Euler\", \"PatankarEuler\"}, optional\n        &lt;TODO&gt;\n    splitting : {\"LieTrotter\", \"Strang\"}, optional\n        &lt;TODO: operator-splitting scheme combining reaction and diffusion\n        sub-steps.&gt;\n    spatial_rate_constants : bool, optional\n        &lt;TODO: when `True`, rate constants vary in space and must be\n        supplied as full spatial arrays.&gt;\n    mode : str or None, optional\n        &lt;TODO&gt;\n\n    Returns\n    -------\n    dict[Species, jax.Array]\n        &lt;TODO: time-series of concentrations, with leading time\n        axis followed by spatial and index axes.&gt;\n\n    Examples\n    --------\n    ```python\n    # &lt;TODO: minimal Gray-Scott or Turing example.&gt;\n    ```\n\n    See Also\n    --------\n    [`solve_well_mixed`][icrn.solve_well_mixed] : Non-spatial counterpart.\n    \"\"\"\n    ops = to_reaction_diffusion_ops(\n        rxns,\n        spatial_dims,\n        dspaces,\n        reaction_solver,\n        splitting,\n        boundary_condition=boundary_condition,\n        mode=mode,\n        spatial_rate_constants=spatial_rate_constants,\n    )\n    combined_vals = (rate_constant_vals, diffusion_constant_vals)\n    return solve_with_ops(\n        ops=ops,\n        state=conc_vals,\n        non_state=combined_vals,\n        dt=dt,\n        times=times,\n        key=key,\n        checkpoint_length=checkpoint_length,\n    )\n</code></pre>"},{"location":"api/#icrn.solve_with_ops","title":"solve_with_ops","text":"<pre><code>solve_with_ops(\n    *,\n    ops,\n    state,\n    non_state,\n    dt,\n    times,\n    key,\n    checkpoint_length,\n)\n</code></pre> <p>Repeatedly apply a sequence of operators.</p> <p>todo</p> <p>Parameters:</p> Name Type Description Default <code>ops</code> <code>sequence</code> <p>&lt;TODO: sequence of operators to apply</p> required <code>state</code> <code>dict[Species, Array]</code> <p> required <code>non_state</code> <code>Any</code> <p> required <code>dt</code> <code>Numeric</code> <p> required <code>times</code> <code>Numeric</code> <p> required <code>key</code> <code>PRNGKey or None</code> <p> required <code>checkpoint_length</code> <code>int or None</code> <p> required <p>Returns:</p> Type Description <code>dict[Species, Array]</code> <p> See Also <p><code>solve_well_mixed</code> <code>solve_reaction_diffusion</code></p> Source code in <code>icrn/solver.py</code> <pre><code>def solve_with_ops(*, ops, state, non_state, dt, times, key, checkpoint_length):\n    \"\"\"Repeatedly apply a sequence of operators.\n\n    todo\n\n    Parameters\n    ----------\n    ops : sequence\n        &lt;TODO: sequence of operators to apply\n    state : dict[Species, jax.Array]\n        &lt;TODO: initial state is updated by the operators.&gt;\n    non_state : Any\n        &lt;TODO: auxiliary data (rate constants, diffusion constants, etc.)\n        passed through unchanged.&gt;\n    dt : Numeric\n        &lt;TODO&gt;\n    times : Numeric\n        &lt;TODO&gt;\n    key : jax.random.PRNGKey or None\n        &lt;TODO&gt;\n    checkpoint_length : int or None\n        &lt;TODO&gt;\n\n    Returns\n    -------\n    dict[Species, jax.Array]\n        &lt;TODO: trajectory.&gt;\n\n    See Also\n    --------\n    [`solve_well_mixed`][icrn.solve_well_mixed]\n    [`solve_reaction_diffusion`][icrn.solve_reaction_diffusion]\n    \"\"\"\n    return _solve_with_ops(\n        ops, state, non_state, dt, key, times, checkpoint_length\n    )\n</code></pre>"},{"location":"developer_notes/","title":"Developer notes","text":"<p>Notes for contributors working on the <code>icrn</code> library itself. For installation and usage examples, see Getting started.</p>"},{"location":"developer_notes/#development-setup","title":"Development setup","text":"<p>Clone the repo and install in editable mode with test tooling:</p> <pre><code>git clone https://github.com/SwissChardLeaf/icrn.git\ncd icrn\npython -m pip install --upgrade pip\npip install -e \".[dev]\"\n</code></pre> <p>Enable pre-commit hooks once:</p> <pre><code>pre-commit install\n</code></pre>"},{"location":"developer_notes/#running-tests","title":"Running tests","text":"<p>Tests use the standard library <code>unittest</code>. The following can be run from the top-level:</p> <pre><code># Full discovery (can be memory-heavy on some machines)\ncoverage run -m unittest discover -s icrn -t . -v\n\n# Target a module or class\npython -m unittest icrn.test_jax\npython -m unittest icrn.test_solver.TestBoundaryCondition\n</code></pre> <p>After a full run, print a summary or open an HTML report locally:</p> <pre><code>coverage report -m\ncoverage html   # open htmlcov/index.html in a browser\n</code></pre> <p>On GitHub Actions, the Tests workflow uploads <code>coverage.xml</code> and <code>htmlcov/</code> as a coverage artifact on each run (download from the workflow summary).</p>"},{"location":"developer_notes/#linting-formatting-and-type-checking","title":"Linting, formatting, and type checking","text":"<p>Quality checks run through pre-commit (see <code>.pre-commit-config.yaml</code>). Run all hooks manually:</p> <pre><code>pre-commit run --all-files\n</code></pre> <p>Configuration lives in <code>pyproject.toml</code>:</p> <p>CI runs the same hooks via the Linting and Tests workflow (<code>.github/workflows/tests_linting.yml</code>).</p>"},{"location":"developer_notes/#building-documentation","title":"Building documentation","text":"<p>Docs are built with MkDocs Material, mkdocstrings, and mkdocs-jupyter. Source pages live in <code>docs/</code>; config is <code>mkdocs.yml</code> at the repo root.</p> <pre><code>pip install -r docs/requirements.txt\npip install -e .\nmkdocs serve          # local preview at http://127.0.0.1:8000\nmkdocs build --strict # writes static site to site/ (gitignored)\n</code></pre> <p>To add a page, create a Markdown file under <code>docs/</code> and register it in the <code>nav</code> section of <code>mkdocs.yml</code>. API docs are generated from NumPy-style docstrings in Python source via <code>docs/api.md</code>.</p> <p>Public functions and classes should use NumPy-style docstrings with Markdown-friendly cross-links (e.g. <code>[`Species`][icrn.Species]</code>). Templates with <code>&lt;TODO&gt;</code> markers remain in some symbols; fill these in as the API stabilizes. mkdocstrings picks up anything listed in <code>docs/api.md</code> that is exported from the package.</p> <p>On push to <code>main</code>, the Docs: build and deploy workflow builds the site and publishes to GitHub Pages.</p>"},{"location":"developer_notes/#continuous-integration","title":"Continuous integration","text":"Workflow When What it does <code>tests.yml</code> push/PR to <code>main</code> <code>unittest discover</code> with coverage; report in logs + artifacts <code>tests_linting.yml</code> push/PR <code>pre-commit run --all-files</code> <code>docs.yml</code> push/PR to <code>main</code> <code>mkdocs build --strict</code>; deploys to GitHub Pages on <code>main</code> <code>publish.yml</code> push tag <code>v*</code> Tests, then build and upload to PyPI (trusted publishing)"},{"location":"getting_started/","title":"Getting started","text":""},{"location":"getting_started/#installation","title":"Installation","text":"<pre><code>pip install icrn\n</code></pre> <p><code>icrn</code> depends on JAX. For GPU or TPU acceleration, follow the JAX install guide for the right wheel for your platform; the default <code>pip install icrn</code> will pull a CPU-only JAX.</p> <p>Python 3.9 or newer is required.</p>"},{"location":"getting_started/#highlights","title":"Highlights","text":"<ul> <li>Indexed reactions: write <code>M[i] + M[j] -&gt; D[i, j]</code> and have it compile to a single <code>jnp.einsum</code>.</li> <li>Well-mixed and reaction\u2013diffusion in one API (<code>solve_well_mixed</code>, <code>solve_reaction_diffusion</code>).</li> <li>Lie\u2013Trotter / Strang operator splitting with spectral diffusion.</li> <li>Fast reactions for limiting-reagent-style annihilation (<code>FastReaction</code>).</li> <li>JAX-native: works under <code>jit</code>, <code>vmap</code>, and <code>grad</code>.</li> <li>Optional non-negativity guards (<code>mode=\"strict\"</code> via <code>checkify</code>, or <code>mode=\"relu\"</code>).</li> </ul>"},{"location":"getting_started/#quick-start","title":"Quick Start","text":""},{"location":"getting_started/#well-mixed-exponential-decay","title":"Well Mixed Exponential Decay","text":"<pre><code>import jax.numpy as jnp\nfrom icrn import many_species, many_rate_constants, MassActionReaction, solve_well_mixed\n\nA = many_species(\"A\")\nk = many_rate_constants(\"k\")\n\nrxns = [MassActionReaction(A, 0, k)]  # A -&gt; 0 with rate k\n\nresult = solve_well_mixed(\n    rxns,\n    conc_vals={A: jnp.array(1.0)},\n    rate_constant_vals={k: jnp.array(1.0)},\n    times=jnp.array([0.0, jnp.log(2), 1.0]),\n    dt=0.005,\n)\nprint(result[A])  # ~ [1.0, 0.5, 0.3679]\n</code></pre>"},{"location":"getting_started/#indexed-reactions","title":"Indexed Reactions","text":"<p>The thing that makes <code>icrn</code> different: species and rate constants can carry indices, and reactions involving them are compiled to a single tensor contraction.</p> <pre><code>import jax.numpy as jnp\nfrom icrn import (\n    many_species, many_rate_constants, many_index_symbols,\n    MassActionReaction, solve_well_mixed,\n)\n\nn = 10\nM, D = many_species(\"M, D\")\nK1, K2 = many_rate_constants(\"K_1, K_2\")\ni, j = many_index_symbols(\"i, j\", n)\n\n# Reversible all-vs-all dimerization\nrxns = [\n    MassActionReaction(M[i] + M[j], D[i, j], K1[i, j]),\n    MassActionReaction(D[i, j], M[i] + M[j], K2[i, j]),\n]\n\nconc = {M: jnp.ones(n), D: jnp.zeros((n, n))}\nrates = {K1: jnp.ones((n, n)) * 0.1, K2: jnp.ones((n, n)) * 0.05}\n\nout = solve_well_mixed(rxns, conc, rates, times=jnp.array([1.0]), dt=1e-3)\n</code></pre>"},{"location":"getting_started/#reactiondiffusion","title":"Reaction\u2013diffusion","text":"<p>A Gray\u2013Scott-style call (omitting the initial-condition loading for brevity):</p> <pre><code>from icrn import solve_reaction_diffusion\n\nU, V = many_species(\"U, V\")\nF, k = many_rate_constants(\"F, k\")\n\nrxns = [\n    MassActionReaction(U + 2 * V, 3 * V, 1),\n    MassActionReaction(V, 0, F + k),\n    MassActionReaction(0, U, F),\n    MassActionReaction(U, 0, F),\n]\n\nsim = solve_reaction_diffusion(\n    rxns,\n    conc_vals={U: U0, V: V0},                       # 2-D fields\n    rate_constant_vals={F: jnp.array(0.037), k: jnp.array(0.06)},\n    diffusion_constant_vals={U: jnp.array(0.2), V: jnp.array(0.1)},\n    times=jnp.array([5e3]),\n    dt=1.0,\n    spatial_dims=(101, 101),\n    dspaces=(1.0, 1.0),\n    mode=\"relu\",\n)\n</code></pre> <p>See the full Gray\u2013Scott, Hopfield-Turing, and winner-take-all tests in <code>icrn/test_solver.py</code> for runnable end-to-end examples.</p>"},{"location":"getting_started/#differentiable-simulation","title":"Differentiable simulation","text":"<p>Because everything is JAX, you can <code>jit</code>, <code>vmap</code>, and <code>grad</code> straight through a solve:</p> <pre><code>import jax\n\n@jax.jit\ndef loss(rate_vals):\n    out = solve_well_mixed(rxns, conc, {k: rate_vals}, times, dt)\n    return jnp.mean((out[A][-1] - target) ** 2)\n\ngrad_fn = jax.grad(loss)\n</code></pre> <p>This is the foundation for training rate constants or initial conditions against a target behavior.</p>"},{"location":"getting_started/#where-to-next","title":"Where to next","text":"<ul> <li>Tutorial notebooks (Colab)</li> <li>The full API reference is in the sidebar.</li> </ul>"},{"location":"tutorials/getting_started/","title":"Getting started","text":"In\u00a0[\u00a0]: Copied! <pre>!uv pip install icrn\n</pre> !uv pip install icrn <pre>Using Python 3.12.11 environment at: /usr\nAudited 1 package in 222ms\n</pre> <p>Confirm that the installation was successful.</p> In\u00a0[\u00a0]: Copied! <pre>!uv pip show icrn\n</pre> !uv pip show icrn <pre>Using Python 3.12.11 environment at: /usr\nName: icrn\nVersion: 0.0.0a0\nLocation: /usr/local/lib/python3.12/dist-packages\nRequires:\nRequired-by:\n</pre> In\u00a0[\u00a0]: Copied! <pre>from icrn import many_index_symbols, many_rate_constants, many_species\n</pre> from icrn import many_index_symbols, many_rate_constants, many_species In\u00a0[\u00a0]: Copied! <pre>A, B = many_species(\"A, B\")\nalpha = many_rate_constants(\"alpha\")\ni, j = many_index_symbols(\"i, j\", 5)\n</pre> A, B = many_species(\"A, B\") alpha = many_rate_constants(\"alpha\") i, j = many_index_symbols(\"i, j\", 5) <p>The parameters to a mass action reaction constructor are reactant, product, and rate constant information, respectively.</p> In\u00a0[\u00a0]: Copied! <pre>from icrn import ICRN, MassActionReaction\n\nrxn = MassActionReaction\n</pre> from icrn import ICRN, MassActionReaction  rxn = MassActionReaction In\u00a0[\u00a0]: Copied! <pre>example_icrn = ICRN(\n    [\n        rxn(\n            0,  # no reactant\n            A[i],  # product is the species A indexed by i\n            alpha[i],  # rate constant alpha, also indexed by i\n        ),\n        rxn(\n            A[j],  # reactant A indexed by j\n            0,  # no product\n            1,  # constant rate of 1\n        ),\n    ]\n)\n</pre> example_icrn = ICRN(     [         rxn(             0,  # no reactant             A[i],  # product is the species A indexed by i             alpha[i],  # rate constant alpha, also indexed by i         ),         rxn(             A[j],  # reactant A indexed by j             0,  # no product             1,  # constant rate of 1         ),     ] ) <p>The icrn can be simulated by creating an Experiment instance with the parameters of interest. The Experiment prepares the functions necessary for simulation using information from the ICRN and the parameters.</p> In\u00a0[\u00a0]: Copied! <pre>from icrn import Experiment\n</pre> from icrn import Experiment In\u00a0[\u00a0]: Copied! <pre>exp_params = {\n    \"dt\": 1e-4,  # dt used for integration method\n    \"batch\": False,  # tensors will not have a leading batch dimension\n    \"integration_method\": \"euler\",  # forward euler integration\n    \"spatial_dim\": None,  # well-mixed simulation\n}\n\nexp = Experiment(example_icrn, exp_params)\n</pre> exp_params = {     \"dt\": 1e-4,  # dt used for integration method     \"batch\": False,  # tensors will not have a leading batch dimension     \"integration_method\": \"euler\",  # forward euler integration     \"spatial_dim\": None,  # well-mixed simulation }  exp = Experiment(example_icrn, exp_params) <pre>compiling forward step function...\ngenerating dynamics expressions\nseparating reactions by timescale\nunifying\nstandardizing\nunifying\nstandardizing\nlambdifying expressions\nno fast reactions\nwell-mixed, unbatched\ndone!\n</pre> <p>Before we begin simulating, we need to specify initial concentrations of the species and the values of the rate constant. The shapes of the concentrations for the species and the rate constant values are implied by the association of index symbols.</p> <p>For example, A[i] implies that the shape of A is (5) because the index set associated with i is {0,...,4}.</p> <p>The concentrations and rate constant values can be manually specified using SJDicts (wrappers to python dictionaries offering convenient functionality). The concentrations and rate constant values should be in JAX arrays.</p> In\u00a0[\u00a0]: Copied! <pre>import jax.numpy as jnp\n\nfrom icrn import SJDict\n</pre> import jax.numpy as jnp  from icrn import SJDict In\u00a0[\u00a0]: Copied! <pre>conc_data = SJDict({A: jnp.ones(5)})\n\nrate_constant_data = SJDict(\n    {  # convert python dict into SJDict\n        alpha: 1 + jnp.arange(5)\n    }\n)\n\nprint(conc_data)\nprint(rate_constant_data)\n</pre> conc_data = SJDict({A: jnp.ones(5)})  rate_constant_data = SJDict(     {  # convert python dict into SJDict         alpha: 1 + jnp.arange(5)     } )  print(conc_data) print(rate_constant_data) <pre>{A: Array([1., 1., 1., 1., 1.], dtype=float32)}\n{alpha: Array([1, 2, 3, 4, 5], dtype=int32)}\n</pre> <p>Or, concentrations and rate constant values can be specified using the dict_builder method which will use the ICRN and broadcasting to create JAX arrays of the correct shape.  Notice that the result of the previous step and this step are the same.</p> In\u00a0[\u00a0]: Copied! <pre>concs_spec = {A: 1}\n\nrate_spec = {alpha: 1 + jnp.arange(5)}\n\n# empty dict given as the final parameter is for diffusion rate,\n# which is irrelevant in a well-mixed simulation\n# we'll talk about that in more detail in a separate tutorial\nconc_data, rate_constant_data, _ = exp.dict_builder(concs_spec, rate_spec, {})\n\nprint(conc_data)\nprint(rate_constant_data)\n</pre> concs_spec = {A: 1}  rate_spec = {alpha: 1 + jnp.arange(5)}  # empty dict given as the final parameter is for diffusion rate, # which is irrelevant in a well-mixed simulation # we'll talk about that in more detail in a separate tutorial conc_data, rate_constant_data, _ = exp.dict_builder(concs_spec, rate_spec, {})  print(conc_data) print(rate_constant_data) <pre>{A: Array([1, 1, 1, 1, 1], dtype=int32, weak_type=True)}\n{alpha: Array([1, 2, 3, 4, 5], dtype=int32)}\n</pre> <p>Now, we can simulate the system for 10 units of time, taking 1000 time points.</p> In\u00a0[\u00a0]: Copied! <pre># empty dict is for diffusion rate, which is irrelevant in\n# a well-mixed simulation\nfinal_concs, concs_hist = exp.simulate_time(\n    conc_data, rate_constant_data, {}, 10.0, sample_num=1000\n)\n</pre> # empty dict is for diffusion rate, which is irrelevant in # a well-mixed simulation final_concs, concs_hist = exp.simulate_time(     conc_data, rate_constant_data, {}, 10.0, sample_num=1000 ) <p>As anticipated, the steady state concentration of A is dictated by their production rates, alpha, essentially [1, 2, 3, 4, 5].</p> In\u00a0[\u00a0]: Copied! <pre>final_concs\n</pre> final_concs Out[\u00a0]: <pre>{A: Array([1.      , 1.999404, 2.998808, 3.998808, 4.997616], dtype=float32)}</pre> In\u00a0[\u00a0]: Copied! <pre>type(final_concs)\n</pre> type(final_concs) Out[\u00a0]: <pre>icrn.dict_utils.SJDict</pre> <p>The other output, the history, contains the initial state and the additional 1000 samples. The leading dimension organizes the history.</p> In\u00a0[\u00a0]: Copied! <pre>type(concs_hist)\n</pre> type(concs_hist) Out[\u00a0]: <pre>icrn.dict_utils.SJDict</pre> In\u00a0[\u00a0]: Copied! <pre>concs_hist[A].shape\n</pre> concs_hist[A].shape Out[\u00a0]: <pre>(1001, 5)</pre> <p>The history can be plotted using matplotlib. If you're not familiar already with matplotlib, there are lots of tutorials online, but it's beyond the scope of this tutorial.</p> In\u00a0[\u00a0]: Copied! <pre>import matplotlib.pyplot as plt\nimport numpy as np\n</pre> import matplotlib.pyplot as plt import numpy as np In\u00a0[\u00a0]: Copied! <pre>for i in range(5):\n    plt.plot(\n        np.arange(0, 1001, 1) * 1e-2, concs_hist[A][..., i], label=f\"$A_{i}$\"\n    )\nplt.legend()\nplt.show()\n</pre> for i in range(5):     plt.plot(         np.arange(0, 1001, 1) * 1e-2, concs_hist[A][..., i], label=f\"$A_{i}$\"     ) plt.legend() plt.show()"},{"location":"tutorials/getting_started/#getting-started-with-icrn","title":"Getting Started with icrn\u00b6","text":"<p>icrn is a python library for the tensor-based and differential simulation of indexed chemical reaction networks.</p>"},{"location":"tutorials/getting_started/#installing-icrn","title":"Installing icrn\u00b6","text":"<p>icrn is registered on PyPi and can be installed using pip.</p>"},{"location":"tutorials/getting_started/#simulating-your-first-icrn","title":"Simulating Your First ICRN\u00b6","text":"<p>An indexed chemical reaction network (ICRN) is composed of chemical species and rate constants related by index symbols. An ICRN can be built programmatically from analogous building blocks. Notice that the index symbols have an associated parameter that specifies the index set. Currently, the index symbols have index sets {0...n-1}, where n can be specified as an input to an index symbol constructor.</p>"},{"location":"tutorials/getting_started/#further-reading","title":"Further Reading\u00b6","text":"<p>Now you are ready to work with icrn!</p> <p>Check out the other tutorials for more information.</p> <ul> <li>Reaction Diffusion Systems</li> <li>ICRN Numerics</li> <li>Training ICRNs</li> <li>Introduction to SJDicts</li> <li>Profiling Computations</li> </ul>"}]}