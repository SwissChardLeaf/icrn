# Getting started

This page is written in Markdown (MyST) and built by Sphinx alongside the rest of the docs.

You can use normal Markdown features: **bold**, *italic*, [links](https://www.sphinx-doc.org/), and lists:

- First item
- Second item

```{code-block} python
import icrn
```

Add more `.md` files under `docs/` and list them (without the `.md` suffix) in the `toctree` in `index.rst`.
